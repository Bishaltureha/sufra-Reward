import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const TopTabScreen = (props: Props) => {
  return (
    <View>
      <Text>TopTabScreen</Text>
    </View>
  );
};

export default TopTabScreen;

const styles = StyleSheet.create({});
